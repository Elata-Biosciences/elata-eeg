[package]
name = "test_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
tracing = "0.1"
uuid = { version = "1.0", features = ["v4"] }

# Local dependencies
eeg_types = { path = "../../crates/eeg_types" }
pipeline = { path = "../../crates/pipeline" }